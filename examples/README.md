# Examples

- `example1`: Basic two-state toggle.
- `example2`: Nested and compound states.
- `example3`: Shared Dictionary context.
- `example4`: Optional Transition Action.
- `example5`: In-game Runtime Debug Panel.

Open the `example.tscn` file inside any folder to run that example.
